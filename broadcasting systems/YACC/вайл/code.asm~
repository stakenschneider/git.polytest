	MOV a, b
	MOV b, c
	MOV c, d[1]
	MOV d, e[12]
:compiled_lable0
	;cycle body
	MOV a, b
	MOV b, c
	MOV c, d[1]
	MOV d, e[12]
:compiled_lable1
	;cycle body
	MOV a, b
	MOV b, c
	MOV c, d[1]
	MOV d, e[12]
	;cycle body - end

	;condition
	MOV eax, b
	DIV eax, d
	MOV tempcell0, eax
	MOV eax, c
	SUB eax, 867
	MOV tempcell1, eax
	MOV eax, tempcell0
	MUL eax, tempcell1
	MOV tempcell2, eax
	MOV eax, 123
	ADD eax, tempcell2
	MOV tempcell3, eax
	MOV eax, tempcell3
	TEST eax
	JNZ compiled_lable1
	;condition end
	;cycle body - end

	;condition
	MOV eax, 4
	SUB eax, d
	MOV tempcell4, eax
	MOV eax, tempcell4
	ADD eax, a
	MOV tempcell5, eax
	MOV eax, 123
	MUL eax, tempcell5
	MOV tempcell6, eax
	MOV eax, b
	DIV eax, d
	MOV tempcell7, eax
	MOV eax, c
	SUB eax, 867
	MOV tempcell8, eax
	MOV eax, tempcell7
	MUL eax, tempcell8
	MOV tempcell9, eax
	MOV eax, tempcell6
	ADD eax, tempcell9
	MOV tempcell10, eax
	MOV eax, tempcell10
	TEST eax
	JZ compiled_lable0
	;condition end
	MOV a, b
	MOV b, c
	MOV c, d[1]
	MOV d, e[12]
:compiled_lable2
	;cycle body
	MOV a, b
	MOV b, c
	MOV c, d[1]
	MOV d, e[12]
	;cycle body - end

	;condition
	MOV eax, b
	DIV eax, d
	MOV tempcell11, eax
	MOV eax, c
	SUB eax, 867
	MOV tempcell12, eax
	MOV eax, tempcell11
	MUL eax, tempcell12
	MOV tempcell13, eax
	MOV eax, 123
	ADD eax, tempcell13
	MOV tempcell14, eax
	MOV eax, tempcell14
	TEST eax
	JB compiled_lable2
	;condition end
